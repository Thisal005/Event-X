apiVersion: v1
kind: Service
metadata:
  name: eventx-backend-service
  labels:
    app: eventx-backend
spec:
  type: ClusterIP
  selector:
    app: eventx-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080